services:
  mongo:
    image: mongo:7
    container_name: opencharity-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - opencharity_mongo:/data/db

volumes:
  opencharity_mongo:
